diff --git a/MQTTService.sedona b/MQTTService.sedona
index 44c6eba..e4e954f 100644
--- a/MQTTService.sedona
+++ b/MQTTService.sedona
@@ -46,7 +46,7 @@ class MQTTService extends Service
   ** Get first child component of type Worker (or subclass).
   ** Returns null if no Worker objects found.
   **
-  private Worker getFirstWorker()
+  Worker getFirstWorker()
   {
     App app = Sys.app
     for (Component c = app.lookup(children); c != null; c = app.lookup(c.nextSibling))
@@ -59,7 +59,7 @@ class MQTTService extends Service
   ** Get next child component after 'worker' that is of type Worker (or subclass).
   ** Returns null if it reaches the end of the list without finding one.
   **
-  private Worker getNextWorker(Worker worker)
+  Worker getNextWorker(Worker worker)
   {
     App app = Sys.app
     for (Component c = app.lookup(worker.id); c != null; c = app.lookup(c.nextSibling))
diff --git a/Message.sedona b/Message.sedona
index 3970b9a..a8a9032 100644
--- a/Message.sedona
+++ b/Message.sedona
@@ -31,6 +31,7 @@ abstract internal class Message extends MessageCommon
   static native void doSubscribe(Obj handle, Str topic, int qos)
   static native void doUnsubscribe(Obj handle, Str topic)
   static native bool fetchData(Obj handle, Str topic, Str buf, int length);
+  static native bool regSlot(Obj handle, Component c, Slot slot, Obj path);
 
   protected virtual Str getPayload() { return null }
 
diff --git a/Worker.sedona b/Worker.sedona
index 363f150..53fbab8 100644
--- a/Worker.sedona
+++ b/Worker.sedona
@@ -286,7 +286,7 @@ abstract internal class Worker extends Component
     return result;
   }
 
-  Obj getHandle()
+  public Obj getHandle()
   {
     if (isSessionLive(handle))
       return handle
diff --git a/kit.xml b/kit.xml
index 3bd297a..20039d0 100644
--- a/kit.xml
+++ b/kit.xml
@@ -28,6 +28,8 @@
   <native qname="communityMQTT::Message.doSubscribe"    id="50::4"  />
   <native qname="communityMQTT::Message.doUnsubscribe"  id="50::5"  />
   <native qname="communityMQTT::Message.fetchData"      id="50::6"  />
+  <native qname="communityMQTT::Message.regSlot"         id="50::8"  />
+  
 
 </sedonaKit>
 
diff --git a/native/communityMQTT_Message.c b/native/communityMQTT_Message.c
index 5f84afd..afe3230 100644
--- a/native/communityMQTT_Message.c
+++ b/native/communityMQTT_Message.c
@@ -112,3 +112,12 @@ Cell communityMQTT_Message_fetchData(SedonaVM* vm, Cell* params)
   buf[minLen] = 0;
   return trueCell;
 }
+
+Cell communityMQTT_Message_regSlot(SedonaVM* vm, Cell* params)
+{
+  uint8_t* self   = params[0].aval;
+  void* slot      = params[1].aval;
+  uint16_t typeId = getTypeId(vm, getSlotType(vm, slot));
+  uint16_t offset = getSlotHandle(vm, slot);
+
+}
\ No newline at end of file
